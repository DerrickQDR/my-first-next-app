export default function Loading() {
    // 这是一个非常简单的加载界面，你可以把它设计得更漂亮（比如加个旋转的圈圈）
    return (
        <div style={{ padding: '20px', color: '#888' }}>
            <h2>⏳ 正在努力获取名言中，请稍候...</h2>
        </div>
    );
}